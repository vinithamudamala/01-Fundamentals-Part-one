/* create an empty array Add 0,1,2 to the array with the push method and array and create the string "0+1+2" out of the array using join method and show the string..*/

array = [];
array.push(0, 1, 2);
console.log(array);
console.log(array.join(""));
